def is_odd(number):
    'Retorna true se um número é ímpar, senão False.'
    return number % 2 != 0


def divide(a_number, other_number):
    'Retorna a divisão de a number por orher number'
    return a_number / other_number
